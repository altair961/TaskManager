<UserControl x:Class="TaskManager.View.ButtonClose"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="0,7,0,-7">
        <Button HorizontalAlignment="Right" Width="30" Height="30" Margin="0,0,5,0" Command="{Binding ButtonCloseCommand}">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Ellipse Stroke="Black" StrokeThickness="0" Fill="LightBlue" />
                        <Label FontSize="18" FontWeight="Bold" FontFamily="Arial" Content="X" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" Height="30" Margin="7,0,0,0" Width="30"/>
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </StackPanel>
</UserControl>